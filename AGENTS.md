# astra-daw-guard

Astra / Codex / Claude Code が DAW を触る前に読むガードレール。

優先:
1. このリポジトリの `SKILL.md` と `policy/deny.txt` を先に読む
2. 既存 MCP / OSC があればそれを使う
3. Computer Use は最後の手段
4. ネットから MIDI / サンプルを黙って取らない
5. プロジェクトを上書き保存しない
6. 作業後は `checklists/after.md` の報告フォーマットで出典を出す

詳細な実装手順は `ROADMAP.md` を上から順に実行する。
